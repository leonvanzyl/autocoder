## AutoCoder project config
## Keep this minimal; engine chains are stored per project in the DB.

commands:
  setup:
    command: "{VENV_PY} -m pip install -r requirements.txt"
  test:
    command: "{VENV_PY} -m pytest -q"

